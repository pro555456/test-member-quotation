<!-- 4) views/verify_register_email.ejs -->
 <%- include('./basic/auth_card.ejs', {
  authWrapClass: '',
  titleText: 'Email 驗證',
  subtitleText: '正在驗證您的註冊 Email，請稍候…',
  body: `
    <div id="subtitle" class="small-muted mb-3">正在驗證中，請稍候…</div>

    <div id="resendBox" class="mt-3" style="display:none;">
      <div class="auth-hr"></div>
      <h5 class="mb-2">重新寄送驗證信</h5>
      <div class="small-muted mb-2">
        若您未收到信、或 token 已過期，可在下方輸入 <b>Email 或 帳號</b> 重新寄送。
      </div>

      <div class="form-group">
        <label>Email（擇一）</label>
        <input id="rEmail" class="form-control" placeholder="name@example.com">
      </div>

      <div class="text-center small-muted my-2">或</div>

      <div class="form-group">
        <label>帳號（擇一）</label>
        <input id="rAccount" class="form-control" placeholder="例：user01 / wilson.tsai">
      </div>

      <div class="text-center">
        <button id="btnResend" class="btn btn-primary" onclick="doResend()">重新寄送</button>
      </div>

      <div class="small-muted mt-3">
        提示：若帳號存在且尚未驗證，系統會寄出驗證信。
      </div>
    </div>

    <div class="small-muted mt-3 text-center">
      驗證完成後會自動跳轉到登入頁。
    </div>
  `
}) %>

<script>
  function showMsg(msg, type='danger', autoHideMs=0) {
    const box = document.getElementById('msgBox');
    box.style.display = 'block';
    box.classList.remove('alert-danger','alert-success','alert-warning','alert-info');
    box.classList.add('alert-' + type);
    box.innerText = msg || '';
    box.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    if (autoHideMs > 0) {
      setTimeout(() => {
        box.style.display = 'none';
        box.innerText = '';
      }, autoHideMs);
    }
  }

  function showSuccess(msg) { showMsg(msg, 'success', 800); }
  function showError(msg) { showMsg(msg, 'danger'); }
  function showInfo(msg) { showMsg(msg, 'info'); }

  function setSubtitle(text) {
    const el = document.getElementById('subtitle');
    if (el) el.innerText = text || '';
  }

  function getQuery(name) {
    const u = new URL(window.location.href);
    return u.searchParams.get(name);
  }

  function getApiErrorMessage(err, fallback='操作失敗') {
    const data = err?.response?.data;
    return data?.message || err?.message || fallback;
  }

  async function apiGet(url, params) {
    const res = await axios.get(url, { params });
    return res.data;
  }

  async function apiPost(url, data) {
    const res = await axios.post(url, data, {
      headers: { 'Content-Type': 'application/json' }
    });
    return res.data;
  }

  function showResendUI(on) {
    const box = document.getElementById('resendBox');
    if (box) box.style.display = on ? 'block' : 'none';
  }

  async function verify() {
    const token = getQuery('token');
    if (!token) {
      setSubtitle('缺少 token，無法驗證');
      showError('缺少 token');
      showResendUI(true);
      return;
    }

    try {
      setSubtitle('正在驗證中，請稍候…');
      showInfo('驗證中…');

      const data = await apiGet('/api/auth/verify-email', { token });

      showSuccess(data?.message || '驗證成功');
      setSubtitle('驗證成功，即將跳轉到登入頁…');

      setTimeout(() => {
        window.location.assign('/login?verified=1');
      }, 850);

    } catch (e) {
      console.error(e);
      const msg = getApiErrorMessage(e, '驗證失敗');

      setSubtitle('驗證失敗');
      showError(msg);
      showResendUI(true);
    }
  }

  async function doResend() {
    const btn = document.getElementById('btnResend');
    btn.disabled = true;

    try {
      const email = (document.getElementById('rEmail').value || '').trim();
      const account = (document.getElementById('rAccount').value || '').trim();

      if (!email && !account) {
        showError('請輸入 Email 或 帳號');
        return;
      }

      setSubtitle('重新寄送驗證信中…');
      showInfo('重新寄送中…');

      const payload = {};
      if (email) payload.email = email;
      if (!email && account) payload.account = account;

      const data = await apiPost('/api/auth/verify-email/resend', payload);

      showSuccess(data?.message || '驗證信已重新寄出');
      setSubtitle('已重新寄出驗證信，請至 Email 收信完成驗證');
    } catch (e) {
      console.error(e);
      const msg = getApiErrorMessage(e, '重新寄送失敗');
      showError(msg);
      setSubtitle('重新寄送失敗');
    } finally {
      btn.disabled = false;
    }
  }

  $(document).ready(() => verify());
</script>